module.exports = {
  // Whether to add parentheses around arrow function parameters (always|avoid). Default: always
  arrowParens: "always",
  // Whether to put `>` on the last line instead of on a new line. Default false
  bracketSameLine: false,
  // Print spaces between brackets in object literals (true|false)
  bracketSpacing: true,
  // Whether to format embedded code snippets in files (auto|off). Default auto
  embeddedLanguageFormatting: "auto",
  // Specify HTML whitespace sensitivity (css|strict|ignore). Default css
  htmlWhitespaceSensitivity: "css",
  // Whether to insert a @format marker at the top of files formatted by Prettier. Default false
  insertPragma: false,
  // Use single quotes instead of double quotes in JSX. Default false
  jsxSingleQuote: false,
  // Maximum line length. Default 80
  printWidth: 80,
  // Line wrapping in prose (always|never|preserve)
  proseWrap: "preserve",
  // When to quote object properties (as-needed|consistent|preserve). Default as-needed
  quoteProps: "as-needed",
  // Only format files with @prettier or @format marker. Default false
  requirePragma: false,
  // Add semicolons at ends of statements
  semi: true,
  // Use single quotes (true|false)
  singleQuote: false,
  // Number of spaces per indentation level. Default 2
  tabWidth: 2,
  // Trailing commas where valid (none|es5|all). Default es5
  trailingComma: "es5",
  // Indent with tabs instead of spaces. Default false (spaces)
  useTabs: false,
  // Whether to indent <style> and <script> tags in Vue files. Default false
  vueIndentScriptAndStyle: false,
};
